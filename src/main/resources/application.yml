spring:

 application:
  name: chatcheck

 datasource:
  url: jdbc:postgresql://localhost:5432/chatcheck
  username: postgres
  password: yntec

 jpa:
  hibernate:
   ddl-auto: update

  show-sql: true

  properties:
   hibernate:
    dialect: org.hibernate.dialect.PostgreSQLDialect

server:
 port: 8080
